import { TimeUntilPipe } from './time-until.pipe';

describe('TimeUntilPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeUntilPipe();
    expect(pipe).toBeTruthy();
  });
});
