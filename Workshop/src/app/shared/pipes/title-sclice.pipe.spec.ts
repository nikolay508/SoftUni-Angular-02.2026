import { TitleSclicePipe } from './title-sclice.pipe';

describe('TitleSclicePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleSclicePipe();
    expect(pipe).toBeTruthy();
  });
});
